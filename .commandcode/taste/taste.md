# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# cli
- Use `python -m utils.main` to run the CLI rather than the installed `dnspython` entry point. Confidence: 0.65

